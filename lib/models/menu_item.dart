import 'package:flutter/material.dart';

class MenuItem {
  final IconData icon;
  final IconData selectedIcon;
  final String title;
  final Color color;

  MenuItem(this.icon, this.selectedIcon, this.title, this.color);
}
